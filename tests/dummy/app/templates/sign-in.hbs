<h2>Sign In</h2>

<form {{on "submit" (prevent-default this.signIn)}}>
  <p>
    <label>Email</label><br>
    <Input @type="email" @value={{this.email}} required />
  </p>

  <p>
    <label>Password</label><br>
    <Input @type="password" @value={{this.password}} required />
  </p>

  {{#if this.error}}
    <p>{{this.error}}</p>
  {{/if}}

  <p>
    <button type="submit">
      Sign In
    </button>
  </p>
</form>