<h1>Items</h1>

<p>
  {{link-to "Add Item" "new"}}
</p>

{{#each model as |item|}}
  <p>{{item.name}} ({{item.count}})</p>

  <p>
    <button {{action "add" item}}>Add</button>
    {{link-to "Edit" "edit" item.id}}
  </p>
{{/each}}